<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/output.css" />
  </head>
  <body class="bg-gray-50">
    <%- include('../partial-/nav') %>
    
    <main class="container mx-auto mt-12 px-4 py-8">
      <div class="max-w-lg mx-auto bg-white rounded-xl shadow-lg p-8">
        <!-- Header -->
        <div class="text-center mb-8">
          <h1 class="text-4xl font-bold text-gray-800 mb-2">Create Account</h1>
          <p class="text-gray-600">Join us and start your journey</p>
        </div>

        <!-- Form -->
        <form action="/Signup" method="POST" class="space-y-5">
          <input type="hidden" name="id" />
        <%- include('../partial-/error') %>

          <!-- Personal Information Section -->
          <div class="space-y-4">
           



            <h2 class="text-sm font-semibold text-gray-700 uppercase tracking-wide">Personal Information</h2>
            
            <div>
              <label for="firstName" class="block text-sm font-medium text-gray-700 mb-1">First Name</label>
              <input
                type="text"
                name="firstName"
                id="firstName"
                value="<%= oldInput.firstName ? oldInput.firstName : '' %>"
                placeholder="John"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                required />
            </div>

            <div>
              <label for="lastName" class="block text-sm font-medium text-gray-700 mb-1">Last Name</label>
              <input
                type="text"
                name="lastName"
                id="lastName"
                value="<%=oldInput.lastName ? oldInput.lastName : '' %>"
                placeholder="Doe"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                required />
            </div>

            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
              <input
                type="email"
                name="email"
                id="email"
                value="<%=oldInput.email ? oldInput.email : ''%>"
                placeholder="example@gmail.com"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                required />
            </div>
          </div>

          <!-- Password Section -->
          <div class="space-y-4 pt-2">
            <h2 class="text-sm font-semibold text-gray-700 uppercase tracking-wide">Security</h2>
            
            <div>
              <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
              <input
                type="password"
                name="password"
                id="password"
                placeholder="Enter your password"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                required />
            </div>

            <div>
              <label for="confirmPassword" class="block text-sm font-medium text-gray-700 mb-1">Confirm Password</label>
              <input
                type="password"
                name="confirm_password"
                id="confirmPassword"
                placeholder="Confirm your password"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent transition duration-200"
                required />
            </div>
          </div>

          <!-- User Type Section -->
          <div class="pt-2">
            <h2 class="text-sm font-semibold text-gray-700 uppercase tracking-wide mb-3">Account Type</h2>
            <div class="flex gap-4">
              <label class="flex items-center space-x-3 cursor-pointer bg-gray-50 rounded-lg px-4 py-3 border-2 border-gray-200 hover:border-red-400 transition duration-200 flex-1">
                <input type="radio" name="userType" value="guest" id="guest" class="w-4 h-4 text-red-500 focus:ring-red-500 " checked=<% oldInput.userType === "guest"  %> />
                <span class="font-medium text-gray-700">Guest</span>
              </label>
              <label class="flex items-center space-x-3 cursor-pointer bg-gray-50 rounded-lg px-4 py-3 border-2 border-gray-200 hover:border-red-400 transition duration-200 flex-1"/>
                <input type="radio" name="userType" value="host" id="host" class="w-4 h-4 text-red-500 focus:ring-red-500" checked=<% oldInput.userType === "host" %>/>
                <span class="font-medium text-gray-700">Host</span>
              </label>
            </div>
          </div>

          <!-- Terms and Conditions -->
          <div class="pt-2">
            <label class="flex items-start cursor-pointer">
              <input type="checkbox" name="terms" id="terms" class="w-4 h-4 mt-1 mr-3 text-red-500 focus:ring-red-500 rounded" required />
              <span class="text-sm text-gray-600">I agree to the <a href="#" class="text-red-500 hover:text-red-600 font-medium">terms and conditions</a></span>
            </label>
          </div>

          <!-- Submit Button -->
          <button
            type="submit"
            class="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-3 px-4 rounded-lg focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2 transition duration-300 text-lg shadow-md mt-6">
             Register Now
          </button>
        </form>

        <!-- Footer -->
        <p class="text-center text-gray-600 text-sm mt-6">
          Already have an account? <a href="/login" class="text-red-500 hover:text-red-600 font-medium">Sign In</a>
        </p>
      </div>
    </main>
  </body>
</html>